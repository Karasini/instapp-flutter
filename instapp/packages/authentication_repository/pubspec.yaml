name: authentication_repository
description: Dart package which manages the authentication domain.
version: 1.0.0

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  retrofit:
  json_annotation:
  equatable: ^1.2.5
  meta: ^1.1.8